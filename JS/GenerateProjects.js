const sectionDataUrl = "./Data/ProjectsData.json"

function renderProjectData(jsonString) {
    let projectData = JSON.parse(jsonString);
    
    projectData.Projects.forEach(Project => {
        
    })
}